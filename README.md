# AISeekhegaIndia Frontend

Next.js App Router + custom MDX docs site for AISeekhegaIndia.

## Setup

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — local server at http://localhost:3000
- `npm run build` — production build
- `npm start` — serve production build
